import { ShowMenuRolePipe } from './show-menu-role.pipe';

describe('ShowMenuRolePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowMenuRolePipe();
    expect(pipe).toBeTruthy();
  });
});

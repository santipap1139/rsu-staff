import { SearchInputAutoPipe } from './search-input-auto.pipe';

describe('SearchInputAutoPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchInputAutoPipe();
    expect(pipe).toBeTruthy();
  });
});

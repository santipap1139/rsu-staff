import { DoubleCheckItemPipe } from './double-check-item.pipe';

describe('DoubleCheckItemPipe', () => {
  it('create an instance', () => {
    const pipe = new DoubleCheckItemPipe();
    expect(pipe).toBeTruthy();
  });
});

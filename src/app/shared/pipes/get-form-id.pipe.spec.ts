import { GetFormIdPipe } from './get-form-id.pipe';

describe('GetFormIdPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFormIdPipe();
    expect(pipe).toBeTruthy();
  });
});

import { CheckCoursePipe } from './check-course.pipe';

describe('CheckCoursePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckCoursePipe();
    expect(pipe).toBeTruthy();
  });
});

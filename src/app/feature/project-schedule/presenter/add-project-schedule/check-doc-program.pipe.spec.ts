import { CheckDocProgramPipe } from './check-doc-program.pipe';

describe('CheckDocProgramPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckDocProgramPipe();
    expect(pipe).toBeTruthy();
  });
});

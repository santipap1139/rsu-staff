import { CheckCoseProgramPipe } from './check-cose-program.pipe';

describe('CheckCoseProgramPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckCoseProgramPipe();
    expect(pipe).toBeTruthy();
  });
});

import { CheckedCourseAddPipe } from './checked-course-add.pipe';

describe('CheckedCourseAddPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckedCourseAddPipe();
    expect(pipe).toBeTruthy();
  });
});

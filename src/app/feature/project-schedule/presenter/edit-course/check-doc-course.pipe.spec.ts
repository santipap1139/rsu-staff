import { CheckDocCoursePipe } from './check-doc-course.pipe';

describe('CheckDocCoursePipe', () => {
  it('create an instance', () => {
    const pipe = new CheckDocCoursePipe();
    expect(pipe).toBeTruthy();
  });
});

import { Component, OnInit, ChangeDetectionStrategy } from '@angular/core';

@Component({
  selector: 'list-project-schedule',
  templateUrl: './list-project-schedule.container.html',
  styleUrls: ['./list-project-schedule.container.scss'],
  // changeDetection: ChangeDetectionStrategy.OnPush
})
export class ListProjectScheduleContainer implements OnInit {

  constructor(
    
  ) { }

  ngOnInit(): void {
  }

}
